import { Dimensions } from 'react-native';
const { width } = Dimensions.get('window');
export const CONTAINER_DIMS = width;
export const CLICK_DURATION = 300;
export const VOTED_ICON_CONTAINER_WIDTH = 80;
