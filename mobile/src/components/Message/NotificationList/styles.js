import { StyleSheet } from 'react-native';
import { RED, WHITE } from 'AppColors';
export const styles = StyleSheet.create({
  delete: {
    width: 100,
    flex: 1,
    alignItems: 'center',
    alignSelf: 'flex-end',
    justifyContent: 'center',
    backgroundColor: RED
  },
  deleteText: {
    color: WHITE
  }
});
