import { WINDOW_WIDTH as width } from 'AppConstants';

export const ANIMATION_DURATION = 50;
export const CONTAINER_DIMS = width;
export const IMAGE_DIMS = CONTAINER_DIMS / 2;
export const CLICK_DURATION = 300;
